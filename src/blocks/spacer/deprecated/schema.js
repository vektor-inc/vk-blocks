export const schema = {
    unit: {
        type: 'string',
        default: 'px',
    },
    pc: {
        type: 'number',
        default: 50,
    },
    tablet: {
        type: 'number',
        default: 10,
    },
    mobile: {
        type: 'number',
        default: 10,
    },
};

export const schemaV3 = {
    anchor: {
        type: 'string',
        default: null,
    },
    unit: {
        type: 'string',
        default: 'px',
    },
    pc: {
        type: 'number',
        default: 40,
    },
    tablet: {
        type: 'number',
        default: 30,
    },
    mobile: {
        type: 'number',
        default: 20,
    },
};
