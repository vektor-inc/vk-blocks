import { ColumnResponsive0491 } from './component';

export default function save( { attributes, className } ) {
	return (
		<ColumnResponsive0491
			attributes={ attributes }
			className={ className }
			for_={ 'save' }
		/>
	);
}
